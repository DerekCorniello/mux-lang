# Mux Compiler

The compiler and CLI crate published as `mux-lang`. It installs the `mux` binary.

**For full documentation, see the [main README](https://github.com/DerekCorniello/mux-lang#readme).**