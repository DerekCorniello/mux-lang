[package]
name = "mux_compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
ordered-float = { version = "4.2", default-features = false, features = ["std"] }
unicode-width = "0.1.11"
lazy_static = "1.4"
# inkwell dependency temporarily disabled - needs LLVM
# inkwell = { version = "0.6.0", features = ["llvm17-0"] }

[dev-dependencies]
insta = { version = "1.43.1", features = ["yaml"] }
